# Implementation Plan

- [ ] Task: Create `etc/prompts/snowglobe.md` with the provided prompt.
- [ ] Task: Create `bin/multipic.py` skeleton with argument parsing (`argparse`).
- [ ] Task: Implement input image resolution logic (handling `--images`, `--dir`, `--characters`).
- [ ] Task: Implement prompt loading from `etc/prompts/`.
- [ ] Task: Implement the GenAI image generation call using the loaded prompt and input images.
- [ ] Task: Implement output saving (image + JSON metadata) and auto-open functionality.
- [ ] Task: Update `skills/how-to-use-gemini-tools/SKILL.md` to document `bin/multipic.py`.
- [ ] Task: Conductor - User Manual Verification (Verify the script works).
